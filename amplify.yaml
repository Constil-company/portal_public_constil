version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd Front
        - npm ci --cache ../.npm --prefer-offline
    build:
      commands:
        - cd Front
        - npm run build
  artifacts:
    baseDirectory: Front/dist
    files:
      - "**/*"
  cache:
    paths:
      - .npm/**/*